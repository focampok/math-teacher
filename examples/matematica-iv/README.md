# Matemática IV (example kit)

- `source/` — original Markdown, practice HTML, formula sheet, express sheet, and `_ensamblar_kit.py`.
- `kit.schema.json` — golden subset used by the renderer tests (5 blocks, weights = 100).
- `dist/index.html` — hand-assembled full kit. Studykit serves it at `/k/matematica-iv`.

The generated subset (schema → Jinja) is at `/k/matematica-iv/generated`.
