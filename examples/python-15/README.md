# Python para principiantes (15 años)

- `source.md` — temario corto (para leer o, más adelante, para el compiler con API key).
- `kit.schema.json` — súbelo en Studykit. El stub lo convierte en un kit jugable.

En local: token `change-me`, archivo `kit.schema.json`, luego publica p. ej. con slug `python-15`.
